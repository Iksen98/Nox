# Nox
This repository contains the CIS Hiring Location Advisor decision agent.

Purpose:
Provide structured, opinionated, governance-aligned COE hiring location recommendations.

This repo is isolated for safe Codex experimentation.

## Quick start

If `pandas` is unavailable in your environment, you can inspect the dataset with the built-in CSV script:

```bash
python inspect_coematrix.py
```

